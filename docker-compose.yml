
services:
    backend:
        build: ./backend
        ports:
            - "4000:4000"
        volumes:
            - sqlite_data:/app/data


    frontend:
        build: ./frontend
        ports:
            - "8080:80"

volumes:
    sqlite_data: